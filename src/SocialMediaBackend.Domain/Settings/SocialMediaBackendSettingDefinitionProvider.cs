using Volo.Abp.Settings;

namespace SocialMediaBackend.Settings;

public class SocialMediaBackendSettingDefinitionProvider : SettingDefinitionProvider
{
    public override void Define(ISettingDefinitionContext context)
    {
        //Define your own settings here. Example:
        //context.Add(new SettingDefinition(SocialMediaBackendSettings.MySetting1));
    }
}
